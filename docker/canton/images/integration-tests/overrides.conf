# overrides.conf — only the exact paths to override (append or load after base)

# Override the whole auth-services array to pin URL + audience (no envs).
canton.participants.participant.ledger-api.auth-services = [
  {
    type             = jwt-jwks
    url              = "https://www.googleapis.com/oauth2/v3/certs"
    target-audience  = "none"
  }
]


