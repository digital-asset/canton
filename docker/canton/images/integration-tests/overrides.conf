# overrides.conf — only the exact paths to override (append or load after base)

# Override the whole auth-services array to pin URL + audience (no envs).
canton.participants.participant.ledger-api.auth-services = [
  {
    type             = jwt-jwks
    url              = "https://www.googleapis.com/oauth2/v3/certs"
    target-audience  = "none"
  }
]

# The default migration paths directory does not exist in the test here.
canton.mediators.mediator.storage.parameters.repeatable-migrations-paths = []
canton.participants.participant.storage.parameters.repeatable-migrations-paths = []
canton.sequencers.sequencer.storage.parameters.repeatable-migrations-paths = []
