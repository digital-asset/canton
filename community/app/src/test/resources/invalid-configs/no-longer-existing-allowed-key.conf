canton {
  parameters.fail-on-unknown-config-keys = false
  parameters.non-standard-config = true

  participants {
    participant1 {
      storage.type = "memory"
      storage.parameters.fail-fast-on-startup = false
      storage.parameters.max-connections = 10

      # No longer existing key
      storage.parameters.ledger-api-jdbc-url = "yes"
    }
  }
}
