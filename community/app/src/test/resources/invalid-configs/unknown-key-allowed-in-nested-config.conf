canton {
  # monitoring is a valid configuration key
  monitoring {
    this-is-not-a-key = "oh no it isn't!!"
  }

  parameters.fail-on-unknown-config-keys = false
  parameters.non-standard-config = true

  # Add this, so the config is valid
  participants {
    participant1 {}
  }
}
