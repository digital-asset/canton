# Traffic Enforcement API

This folder contains the protobuf definitions for the Traffic Enforcement API.
The `scala-impl` folder contains an implementation of it, but the participant is agnostic of the underlying implementation,
and relies only on this API to drive traffic decisions.
