{
  transaction-sizes-and-weights = // Mandatory but unused by the short-circuit binding
  [
    {
      size-bytes = 37,
      weight = 1,
    },
  ],
  run-duration = 1 millisecond,
  per-node-write-period = 1 millisecond,
  reporting-interval = 10 seconds,
  nodes = [
    {
      type = in-process-read-write-node,
      host = "",
      read-port = "",
      write-port = "",
    }
  ]
}
