canton {
  features {
    // we need to turn on some extra features which aren't stable yet
    enable-preview-commands = yes
    enable-testing-commands = yes
  }
  monitoring {
    // turn on distributed tracing
    tracing.propagation = enabled
  }
}
include "config/banking.conf"
include "config/medical.conf"
include "config/participant1.conf"
include "config/participant2.conf"
include "config/participant3.conf"
include "config/participant4.conf"
include "config/participant5.conf"
include "config/participant6.conf"
