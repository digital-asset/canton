### Setup Git Hooks

To install all hooks, run the following command

```sh
git config core.hooksPath .hooks
```

To install only some hooks, create symlinks in `.git/hooks` pointing to the hooks you want to install.
